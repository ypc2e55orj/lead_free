#ifndef INTERVAL_H
#define INTERVAL_H

// libc
#include <stdint.h>

/**
 * @brief Start interval process
 */
void INTERVAL_Start();
/**
 * @brief Buzzer
 */
void INTERVAL_Buzzer(int16_t ms);

#endif // INTERVAL_H
